pipeline {
    agent any

    stages {
        stage ('Compile Stage') 
        {
            steps { git branch: 'Dev', url: 'https://github.com/chaitanyarayudu/jenkins-example.git' }
        }
    }
}
